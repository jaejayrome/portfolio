<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ resume.name }}{% endblock %}</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    
    <style>
        /* Theme Dictionaries */
        :root { 
            --crust: #11111b; --mantle: #181825; --base: #1e1e2e;
            --surface0: #313244; --text: #cdd6f4; --subtext0: #a6adc8;
            --blue: #89b4fa; --green: #a6e3a1; --mauve: #cba6f7;
            --red: #f38ba8; --yellow: #f9e2af; --border: #313244;
        }
        
        [data-theme="gruvbox"] {
            --crust: #1d2021; --mantle: #282828; --base: #32302f;
            --surface0: #504945; --text: #ebdbb2; --subtext0: #a89984;
            --blue: #83a598; --green: #b8bb26; --mauve: #d3869b;
            --red: #cc241d; --yellow: #fabd2f; --border: #504945;
        }

        [data-theme="nord"] {
            --crust: #292e39; --mantle: #2e3440; --base: #3b4252;
            --surface0: #4c566a; --text: #eceff4; --subtext0: #d8dee9;
            --blue: #81a1c1; --green: #a3be8c; --mauve: #b48ead;
            --red: #bf616a; --yellow: #ebcb8b; --border: #4c566a;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { 
            font-family: 'JetBrains Mono', monospace; 
            background: var(--crust); color: var(--text); 
            padding: 2rem 1rem; display: flex; flex-direction: column; align-items: center;
        }
        
        .cmd-line {
            width: 100%; max-width: 1200px; margin-bottom: 1rem;
            color: var(--subtext0); display: flex; gap: 0.5rem;
        }
        .cmd-line select {
            background: transparent; color: var(--green); border: none;
            font-family: inherit; font-size: 1rem; cursor: pointer; outline: none;
        }

        /* The Dynamic Workspace Setup */
        .workspace {
            display: flex; flex-direction: column;
            width: 100%; max-width: 1200px; 
            transition: all 0.4s ease;
        }
        
        .main-pane {
            min-width: 0;
            flex: 1;
            transition: all 0.4s ease;
            display: flex; flex-direction: column;
        }

        .split-pane {
            min-width: 0;
            opacity: 0;
            overflow: hidden;
            border: 0px solid var(--border); /* Hidden initially */
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
            display: flex; flex-direction: column;
        }

        /* --- DESKTOP BEHAVIOR --- */
        @media (min-width: 800px) {
            .workspace { flex-direction: row; }
            .split-pane { max-width: 0; margin-left: 0; }
            
            /* When split is triggered on desktop */
            .workspace.split-active .split-pane {
                max-width: 50%;
                margin-left: 1rem;
                opacity: 1;
                border-width: 1px;
            }
        }

        /* --- MOBILE BEHAVIOR --- */
        @media (max-width: 799px) {
            .split-pane { max-height: 0; margin-top: 0; }
            
            /* When split is triggered on mobile */
            .workspace.split-active .split-pane {
                max-height: 2000px; 
                margin-top: 1rem;
                opacity: 1;
                border-width: 1px;
            }
        }

        /* Terminal UI Styles */
        .terminal-window { 
            background: var(--base); border-radius: 8px;
            border: 1px solid var(--border);
        }
        .split-pane.terminal-window {
            border: none; /* Let the transition handle the border */
        }
        .workspace.split-active .split-pane.terminal-window {
            border: 1px solid var(--border);
        }
        
        .terminal-header {
            background: var(--mantle); padding: 8px 16px;
            border-bottom: 1px solid var(--border); color: var(--subtext0);
            font-size: 0.85rem; display: flex; justify-content: space-between; align-items: center;
        }
        .terminal-body { padding: 1.5rem; overflow-y: auto; flex: 1; }

        a { color: var(--blue); text-decoration: none; }
        a:hover { text-decoration: underline; }
        
        .vsplit-btn {
            background: transparent; color: var(--mauve); border: none;
            font-family: inherit; cursor: pointer; font-size: 0.9em;
            margin-top: 0.5rem; text-align: left;
        }
        .vsplit-btn:hover { text-decoration: underline; }
        .vsplit-btn::before { content: "› :vsplit "; color: var(--subtext0); }
        
        /* Close Buttons */
        .close-btn { background: transparent; border: none; cursor: pointer; font-family: inherit; }
        .close-btn:hover { opacity: 0.8; }
    </style>
</head>
<body>

    <div class="cmd-line">
        <span>:colorscheme</span>
        <select id="themeSelector" onchange="document.documentElement.setAttribute('data-theme', this.value)">
            <option value="mocha">catppuccin-mocha</option>
            <option value="gruvbox">gruvbox-dark</option>
            <option value="nord">nord</option>
        </select>
    </div>

    <div class="workspace" id="workspace">
        {% block content %}{% endblock %}
    </div>

</body>
</html>
